{
  "definitions": {},
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://example.com/root.json",
  "type": "object",
  "title": "The Root Schema",
  "required": [
    "errors"
  ],
  "properties": {
    "errors": {
      "$id": "#/properties/errors",
      "type": "object",
      "title": "The Errors Schema",
      "required": [
        "password_confirmation"
      ],
      "properties": {
        "password_confirmation": {
          "$id": "#/properties/errors/properties/password_confirmation",
          "type": "array",
          "title": "The Password_confirmation Schema",
          "items": {
            "$id": "#/properties/errors/properties/password_confirmation/items",
            "type": "string",
            "title": "The Items Schema",
            "default": "",
            "examples": [
              "doesn't match Password"
            ],
            "pattern": "^(.*)$"
          }
        }
      }
    }
  }
}
